<%=form_tag('/products', method: :post, remote: true) do %>
    <%= text_field_tag("product[name]", nil, placeholder: 'Новый продукт') %>
    <%= submit_tag("Добавить", class: "btn btn-lg btn-primary", remote: true) %>
<% end %>