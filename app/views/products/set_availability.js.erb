<% case URI(request.referer).path %>
<% when "/products" %>
	$("#products_lists").html("<%= escape_javascript(render('products/two_columns_products_lists')) %>")
<% when "/courses" %>
	$("#courses_lists").html("<%= escape_javascript(render('courses/two_columns_courses_lists')) %>")
<% end %>