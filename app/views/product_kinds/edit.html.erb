<% provide(:title, 'Изменить вид продукта') %>

<%= form_for(@product_kind) do |f| %>
<%= render 'shared/error_messages', object: f.object %>

<%= f.label :name, "Название" %>
<%= f.text_field :name %>

<%= f.submit "Сохранить", class: "btn btn-lg btn-primary" %>
<% end %>