<%=form_tag('/menu_kinds', method: :post, remote: true) do %>
    <%= text_field_tag("menu_kind[name]", nil, placeholder: 'Новый вид меню') %>
    <%= submit_tag("Добавить", class: "btn btn-lg btn-primary", remote: true) %>
<% end %>