<section>
	<% if current_user.courses && current_user.courses.any? %>
	<h2>Блюда (<%= current_user.courses.count %>)</h2>
	<ol class="courses">
		<%= render current_user.courses %>
	</ol>
	<% end %>
</section>

